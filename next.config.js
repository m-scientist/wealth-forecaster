/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Turn off telemetry and enable custom bundle options if needed
  telemetry: false,
};

export default nextConfig;
